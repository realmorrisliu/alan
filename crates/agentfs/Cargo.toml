[package]
name = "alan-agentfs"
description = "alan-agentfs — the projection file server that maps the Agent Execution Engine's session/event stream onto the agent file layout served at /agent (the alan-agent-adapter-contract)."
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
alan-ap = { path = "../ap" }
alan-agent-protocol.workspace = true
async-trait.workspace = true
serde_json.workspace = true
tokio = { workspace = true }
