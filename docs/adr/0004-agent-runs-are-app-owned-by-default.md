# Agent Runs Are App-Owned By Default

Agent Runs should be owned by the app, object, or task that requested them by
default, while the System Agent Supervisor provides continuity across runs. This
keeps UPDF reading assistance, Groove Master practice help, and Alan Agent tasks
inside their product semantics instead of making every AI action a
supervisor-owned global task.
