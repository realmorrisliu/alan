# Agent Tool Governance Becomes Command Governance

Alan OS should generalize the existing Alan Agent tool governance model
into Command Governance for all OS commands. The current allow, deny,
escalate policy engine, approval checkpoints, audit metadata, and sandbox
backend ideas remain valuable, but they should become the source material for a
broader OS layer that governs shell commands, app commands, domain
actions, and agent-proposed commands.
