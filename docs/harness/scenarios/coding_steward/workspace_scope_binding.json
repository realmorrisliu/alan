{
  "id": "coding_steward/workspace_scope_binding",
  "category": "coding_steward",
  "description": "Validate that nested cwd execution stays distinct from the bound workspace root in steward-to-worker handoff.",
  "blocking": true,
  "command": "cargo test -p alan-agent-engine runtime::virtual_tools::tests::test_try_handle_virtual_tool_call_invoke_delegated_skill_keeps_workspace_root_separate_from_nested_cwd --lib -- --exact && cargo test -p alan-agent-engine runtime::child_agents::tests::child_workspace_root_uses_parent_workspace_instead_of_nested_tool_cwd --lib -- --exact",
  "assertions": [
    "delegated launch preserves the nested cwd for execution",
    "workspace root remains the repo boundary rather than the nested cwd",
    "parent workspace binding is stable across child launch resolution"
  ],
  "kpi_tags": [
    "workspace_scope",
    "nested_cwd",
    "path_boundary"
  ]
}
