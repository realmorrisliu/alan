{
  "id": "compaction/hard_threshold_without_flush",
  "category": "compaction",
  "description": "Validate hard-threshold automatic compaction bypasses pre-compaction memory flush and leaves latest_memory_flush_attempt empty.",
  "blocking": true,
  "command": "cargo test -p alan-agent-engine --lib runtime::transition::tests::test_auto_pre_turn_hard_compaction_skips_memory_flush -- --exact",
  "assertions": [
    "hard-threshold automatic compaction emits no memory_flush_observed event",
    "latest_memory_flush_attempt stays empty in Agent Machine and rollout evidence",
    "compaction pressure level remains hard with no linked memory flush id"
  ],
  "kpi_tags": [
    "hard_threshold",
    "memory_flush_bypass",
    "surface_consistency"
  ]
}
