{
  "id": "autonomy/dedup_side_effect",
  "category": "autonomy",
  "description": "Validate side-effect idempotency dedupe for process effects and skip duplicate execution.",
  "blocking": true,
  "command": "cargo test -p alan-agent-engine runtime::tool_orchestrator::tests::test_side_effect_dedupe_for_process_effects -- --exact",
  "assertions": [
    "same process side effect does not execute twice",
    "dedupe replay returns prior tool payload",
    "effect dedupe decision remains auditable"
  ],
  "kpi_tags": [
    "dedupe",
    "idempotency",
    "side_effect_safety"
  ]
}
