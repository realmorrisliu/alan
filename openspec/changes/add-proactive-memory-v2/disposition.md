# Disposition — 2026-09-19

Parked pending Machine/evidence ownership. System Store and explicit Memory Store mounts replace Host-directory inference. Replan runtime proposal versus Store commit authority and file-native inspection; no automatic policy/permission learning.

Do not execute the retained tasks until an explicit new planning decision
replaces the affected design and deltas. Remaining artifacts are a design
inventory, not implementation authorization or accepted canonical behavior.
