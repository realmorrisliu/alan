# Residual Compatibility Inventory

Inventory captured from merged `main` at `80d04c317804d1b14a25ef72305f969fe2aaa48e`
before deleting any retired type or declaration.

| Surface | Current producer / parser | Repository-owned positive fixtures | Existing rejection fixtures | Hard-cut work |
| --- | --- | --- | --- | --- |
| LLMFS request | `crates/llmfs/src/lib.rs` owns the untagged `RequestDoc`, `LegacyRequestDoc`, `WireRequestDocV1`, and commit-time mapping. | `crates/llmfs/tests/generation.rs` has one complete v1 mapping test and 19 unversioned request writes used by generation lifecycle tests. | Empty and malformed documents, empty v1 messages, and unknown fields are rejected; missing and unknown versions are not yet covered explicitly. | Convert every positive write to v1, deserialize only `WireRequestDocV1`, and prove rejected documents never dispatch. |
| Agent config | `crates/agent-engine/src/config.rs` still parses the singular compaction threshold and a renamed deprecated thinking-budget field; `persisted_workspace_config.rs` and `runtime/engine.rs` still recover the singular threshold. | Config tests still exercise the singular threshold as accepted input; runtime tests construct the persisted compatibility field. | The deprecated thinking-budget key already errors through a dedicated field, while the singular compaction field still has compatibility-conflict tests instead of unknown-field rejection. | Remove both serde fields and persisted fallback, retain only soft/hard thresholds and canonical reasoning effort, and use `deny_unknown_fields` for hard rejection. |
| Governance profile | `crates/agent-protocol/src/op.rs` has a custom deserializer accepting four retired spellings as `Autonomous`. | Repository producers already construct `GovernanceProfile::Autonomous`; no retired JSON producer exists outside the compatibility test. | Typos and wrong-typed values are rejected, but two retired spellings are currently asserted as accepted and two have no explicit fixture. | Derive/implement canonical-only deserialization and reject every retired spelling plus malformed and wrong-typed values. |
| Input operation | `crates/agent-protocol/src/op.rs` aliases `type: steer` to `Input` and defaults/omits `Input.mode`. | Rust producers construct `Op::Input` with an explicit mode, but serialization tests currently expect the default mode to be omitted. | Protocol tests currently assert missing mode and retired `steer` both succeed. | Remove alias/default/skip logic, emit explicit mode, and invert those fixtures into hard rejections. |
| Apple palette | `clients/apple/alan-macos/Support/ShellDesignTokens.swift` defines `rootBacking`, `canvas`, and `window` aliases. | One `rootBacking` call site remains in `TerminalPaneView.swift`; four `window` call sites remain in material resolution; `canvas` has no caller. | Apple focused tests cover the affected shell surface code; source search is the deletion proof. | Move callers to `ShellPaper.root` / `ShellPaper.window`, then delete all three aliases. |
| Workspace dependencies | Root `Cargo.toml` alone declares `tokio-stream`, `bytes`, `tracing-appender`, `tower`, `tower-http`, `config`, and `terminal_size`. `cargo metadata --locked --no-deps` reports no package owner for any of them. | None. | Source and manifest searches show no workspace package declaration or code owner. | Delete the seven unused workspace declarations. |
| LLM test Axum features | Root `Cargo.toml` enables Axum `ws`; `crates/llm/Cargo.toml` inherits it only for HTTP/SSE test servers in `chatgpt_responses.rs`. | The ChatGPT Responses tests use Router, JSON, HTTP status/header, SSE responses, and `serve`. | No LLM test imports Axum WebSocket types. | Make the LLM dev dependency explicitly featureless and confirm WebSocket-only resolved packages disappear when unowned. |
