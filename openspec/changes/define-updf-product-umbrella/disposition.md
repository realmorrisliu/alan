# Disposition — 2026-09-19

Parked. Alan for macOS preview is cancelled because the desktop product is
retired. Keep domain/package ideas only if a concrete external viewer need is
selected; this is not a cognition or terminal-integration prerequisite.
Related tasks and the preview delta remain as unchecked historical inventory;
the delta must not be synced.

Do not execute the retained tasks until an explicit new planning decision
replaces the affected design and deltas. Remaining artifacts are a design
inventory, not implementation authorization or accepted canonical behavior.
