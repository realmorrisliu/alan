# Disposition — 2026-09-19

Parked for redesign. Preserve durable history discovery, but the renderer-only /mnt/agent-runtime/clone launch exception in the old draft is rejected. ADR-0038/0048 command-plane launch remains authoritative. Next planning must separate history quota, durability and launch rights.

Do not execute the retained tasks until an explicit new planning decision
replaces the affected design and deltas. Remaining artifacts are a design
inventory, not implementation authorization or accepted canonical behavior.
